version: "3.9"
services:
  app-test:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: ["bash", "-lc", "python3 scripts/validate_test_traceability.py && pytest -q"]
