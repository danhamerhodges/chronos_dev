interface:
  display_name: "GH Fix CI"
  short_description: "Triage failing GitHub Actions checks and drive minimal fixes"
  default_prompt: "Inspect failing GitHub PR checks, extract actionable GitHub Actions failures, propose a minimal fix plan, and implement after approval."
policy:
  allow_implicit_invocation: false
