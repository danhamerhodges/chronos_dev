interface:
  display_name: "PR Validation Orchestrator"
  short_description: "Advisory PR checks for sizing and traceability"
  default_prompt: "Run PR-level commit sizing and traceability validation, then summarize merge readiness."
policy:
  allow_implicit_invocation: true
