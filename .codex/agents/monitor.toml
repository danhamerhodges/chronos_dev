model = "gpt-5.3-codex"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = "Monitoring role for long-running checks and polling. Report concise status updates and blockers only."
